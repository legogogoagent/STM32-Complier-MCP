# 架构文档 - STM32 MCP Build Server

> **版本**: v1.0 (当前架构)
> **日期**: 2026-02-13
> **状态**: 即将重构为 v2.0 (uvx + Docker)

---

## 目录

1. [当前架构 (v1.0)](#当前架构-v10)
2. [v1.0 的问题](#v10-的问题)
3. [目标架构 (v2.0)](#目标架构-v20)
4. [重构决策记录](#重构决策记录)
5. [迁移计划](#迁移计划)

---

## 当前架构 (v1.0)

### 概览

v1.0 采用"复制代码到用户项目"的方式安装，MCP Server 以 Python 模块形式运行。

```
用户项目/
├── .opencode/
│   ├── mcp.json                    ← MCP配置（硬编码路径）
│   └── skills/
│       └── stm32-dev-workflow/     ← Skill文件
├── mcp_build/                      ← 复制进来的Build MCP代码
│   ├── stm32_build_server.py
│   └── gcc_parse.py
├── mcp_flash/                      ← 复制进来的Flash MCP代码
│   ├── stm32_flash_server.py
│   └── esp32_remote_flasher.py
├── requirements.txt                ← 复制进来的Python依赖
└── 用户的STM32代码/
```

### 安装流程 (v1.0)

```bash
# 1. 克隆仓库
git clone https://github.com/legogogoagent/STM32-Complier-MCP.git /tmp/stm32-mcp

# 2. 复制skill
cp -r /tmp/stm32-mcp/.opencode/skills/stm32-dev-workflow .opencode/skills/

# 3. 复制MCP Server代码到用户项目（非标准做法）
cp -r /tmp/stm32-mcp/mcp_build ./
cp -r /tmp/stm32-mcp/mcp_flash ./
cp /tmp/stm32-mcp/requirements.txt ./

# 4. 安装Python依赖（污染用户环境）
pip install -r requirements.txt

# 5. 配置MCP
cat > .opencode/mcp.json << 'EOF'
{
  "mcpServers": {
    "stm32-build": {
      "command": "python3",
      "args": ["-m", "mcp_build.stm32_build_server"],
      "cwd": "{PROJECT_ROOT}",
      "env": { "PYTHONPATH": "{PROJECT_ROOT}" }
    }
  }
}
EOF

# 6. 本地构建Docker编译镜像（5-10分钟）
docker build -f docker/Dockerfile -t stm32-toolchain:latest .
```

### 组件关系

```
┌───────────────────────────────────────────────────────────┐
│                    用户项目目录                             │
│                                                           │
│  ┌──────────────┐   python -m   ┌──────────────────────┐  │
│  │ .opencode/   │──────────────►│ mcp_build/           │  │
│  │  mcp.json    │               │  stm32_build_server  │  │
│  └──────────────┘               └──────────┬───────────┘  │
│                                            │              │
│                                   docker run              │
│                                            │              │
│                                 ┌──────────▼───────────┐  │
│                                 │ stm32-toolchain      │  │
│                                 │ (本地构建的镜像)       │  │
│                                 │ arm-none-eabi-gcc     │  │
│                                 └──────────────────────┘  │
└───────────────────────────────────────────────────────────┘
```

---

## v1.0 的问题

### 来源：Issue #1 用户真实反馈

#### 🔴 P0 - 阻塞性问题

| # | 问题 | 根本原因 | 影响 |
|---|------|---------|------|
| 1 | ARM工具链缺失 | 用户环境无 arm-none-eabi-gcc，无sudo权限 | 需手动下载230MB，耗时7-10分钟 |
| 2 | Makefile不兼容 | STM32CubeIDE生成Windows路径 + GCC 13.x不兼容选项 | 编译失败，需手动修3个文件 |

#### 🟡 P1 - 体验问题

| # | 问题 | 根本原因 | 影响 |
|---|------|---------|------|
| 3 | 头文件大小写敏感 | Windows不区分大小写，Linux严格区分 | 编译失败，错误信息不够清晰 |
| 4 | Docker构建超时 | 无预构建镜像，每次本地构建 | 首次构建5-10分钟 |
| 5 | 安装步骤繁琐 | 6个手动步骤 | 预期<5分钟，实际>20分钟 |

#### 🏗️ 架构层面问题

| 问题 | 说明 |
|------|------|
| **往用户项目复制代码** | 违反MCP行业标准，没有任何主流MCP这样做 |
| **pip install 污染环境** | 安装依赖到用户系统Python环境 |
| **mcp.json 路径硬编码** | 绝对路径，不可移植 |
| **Docker镜像本地构建** | 慢，不可重复，版本不一致 |

#### 行业对比

| MCP Server | 安装方式 | 是否复制代码到项目 | 是否pip/npm install |
|------------|---------|-------------------|-------------------|
| **Playwright MCP** | `npx -y @playwright/mcp` | ❌ 不复制 | ❌ 自动隔离 |
| **SQLite MCP** | `uvx mcp-server-sqlite` | ❌ 不复制 | ❌ 自动隔离 |
| **Filesystem MCP** | `npx -y @modelcontextprotocol/server-filesystem` | ❌ 不复制 | ❌ 自动隔离 |
| **我们 (v1.0)** | `cp -r mcp_build/ 用户项目/` | ⚠️ 复制 | ⚠️ pip install |

---

## 目标架构 (v2.0)

### 设计原则

1. **零污染** - 不修改用户项目，不改变用户Python环境
2. **行业标准** - 使用 `uvx` (Python MCP标准) + Docker (编译隔离)
3. **一键安装** - 用户只需配置 `mcp.json`，无需其他步骤
4. **预构建镜像** - Docker Hub 提供多架构、多版本镜像

### 目标安装流程

```bash
# 用户只需要做一件事：配置 mcp.json
{
  "mcpServers": {
    "stm32": {
      "command": "uvx",
      "args": ["stm32-mcp"]
    }
  }
}

# uvx 自动从 PyPI 下载并在隔离环境运行，零污染
# stm32-mcp 内部自动 docker pull 编译镜像（首次）
```

### 目标组件架构

```
┌─────────────────────────────────────────────────────────────┐
│                      用户环境                                │
│                                                             │
│  mcp.json                                                   │
│  "command": "uvx", "args": ["stm32-mcp"]                   │
│       │                                                     │
│       ▼                                                     │
│  ┌──────────────────────────────────────────────┐           │
│  │  uvx 隔离环境 (不污染系统Python)              │           │
│  │                                              │           │
│  │  stm32-mcp (PyPI包)                          │           │
│  │  ├── server.py      (MCP Server, build+flash)│           │
│  │  ├── gcc_parse.py   (错误解析器)              │           │
│  │  └── docker_runner.py (Docker调用)            │           │
│  └──────────────────┬───────────────────────────┘           │
│                     │                                       │
│            docker run --rm                                  │
│            -v workspace:/src:ro                             │
│            -v /tmp/out:/out                                 │
│                     │                                       │
│  ┌──────────────────▼───────────────────────────┐           │
│  │  legogogoagent/stm32-toolchain:12.3          │           │
│  │  (预构建镜像, Docker Hub)                     │           │
│  │                                              │           │
│  │  ├── arm-none-eabi-gcc 12.3                  │           │
│  │  ├── make                                    │           │
│  │  └── build.sh (含Makefile自动修复)            │           │
│  │                                              │           │
│  │  支持架构: x86_64 + ARM64                     │           │
│  └──────────────────────────────────────────────┘           │
│                                                             │
│  用户项目/ (完全不修改)                                      │
│  ├── Core/                                                  │
│  ├── Makefile                                               │
│  └── *.c / *.h                                              │
└─────────────────────────────────────────────────────────────┘
```

### PyPI 包结构

```
stm32-mcp/
├── pyproject.toml              ← 包配置
├── src/
│   └── stm32_mcp/
│       ├── __init__.py
│       ├── __main__.py         ← uvx 入口点
│       ├── server.py           ← 合并的 MCP Server (build + flash)
│       ├── gcc_parse.py        ← GCC/LD 错误解析器
│       └── docker_runner.py    ← Docker 容器管理
├── docker/
│   └── Dockerfile              ← 多架构编译镜像
└── tests/
```

### Docker 镜像规格

| 属性 | 值 |
|------|-----|
| 镜像名 | `legogogoagent/stm32-toolchain` |
| 标签 | `12.3`, `13.2`, `latest` |
| 架构 | `linux/amd64` + `linux/arm64` |
| 基础镜像 | Ubuntu 24.04 |
| 工具链 | arm-none-eabi-gcc |
| 内置脚本 | build.sh (含Makefile自动修复) |

### Makefile 自动修复 (build.sh 内)

在Docker容器内、编译前自动执行：

```bash
# 1. 复制Makefile到临时目录
cp /src/Makefile /tmp/build/Makefile

# 2. 修复Windows绝对路径 → 相对路径
sed -i 's|C:\\\\[^ ]*\\\\||g' /tmp/build/Makefile
sed -i 's|C:/[^ ]*/||g' /tmp/build/Makefile

# 3. 移除GCC 13.x不兼容选项
sed -i 's/-fcyclomatic-complexity//g' /tmp/build/Makefile
sed -i 's/-fstack-usage//g' /tmp/build/Makefile

# 4. 用修复后的Makefile编译
make -f /tmp/build/Makefile -C /src -j8
```

### 头文件大小写处理

不自动修复，但在GCC错误解析器中增强提示：
```
❌ fatal error: crc16_dnp.h: No such file or directory

💡 提示: 检测到可能的大小写不匹配
   代码中: #include "crc16_dnp.h"
   实际文件: CRC16_DNP.h
   建议: 修改 #include 为 "CRC16_DNP.h"
```

---

## 重构决策记录

### 决策1: Docker编译 vs 本地工具链

- **决策**: 使用Docker编译
- **原因**: 不改变用户环境，环境一致性保证
- **日期**: 2026-02-13

### 决策2: 镜像获取策略

- **决策**: 先本地构建(D)，后提供Docker Hub预构建镜像(A)
- **原因**: 渐进式改进，首次可用，后续更快
- **镜像**: `legogogoagent/stm32-toolchain:12.3`
- **架构**: x86_64 + ARM64
- **日期**: 2026-02-13

### 决策3: Makefile修复方式

- **决策**: Docker容器内临时副本修复，build.sh中用sed
- **原因**: 不修改用户文件，符合"MCP只编译不改代码"原则
- **日期**: 2026-02-13

### 决策4: 头文件大小写

- **决策**: 不自动修复，增强错误提示
- **原因**: 本质是用户代码bug，自动修复可能引入新问题
- **日期**: 2026-02-13

### 决策5: MCP Server分发方式

- **决策**: uvx + Docker (方案B)
- **备选**: 全Docker(A), npx+Docker(C)
- **原因**: Python MCP标准方式，不污染用户环境
- **日期**: 2026-02-13

### 决策6: PyPI包结构

- **决策**: 单包 `stm32-mcp`，包含build + flash
- **备选**: 两个包 `stm32-mcp-build` + `stm32-mcp-flash`
- **原因**: 简化安装，用户只需配置一个MCP Server
- **日期**: 2026-02-13

---

## 迁移计划

### Phase 1: 文档和分支准备 (当前)

- [x] 创建 ARCHITECTURE.md (本文档)
- [x] 更新 README.md 标注 v1.0
- [x] 回复 Issue #1
- [x] 创建 `feature/uvx-docker-refactor` 分支

### Phase 2: PyPI包重构

- [ ] 重组代码为标准 Python 包结构
- [ ] 创建 `pyproject.toml`
- [ ] 合并 build + flash MCP Server
- [ ] 实现 `docker_runner.py`（Docker镜像管理）
- [ ] 本地测试 `uvx stm32-mcp`

### Phase 3: Docker镜像优化

- [ ] 优化 Dockerfile（多阶段构建）
- [ ] 集成 build.sh Makefile自动修复
- [ ] 多架构构建 (x86_64 + ARM64)
- [ ] 推送到 Docker Hub

### Phase 4: 发布

- [ ] 发布到 PyPI (`stm32-mcp`)
- [ ] 推送 Docker 镜像 (`legogogoagent/stm32-toolchain:12.3`)
- [ ] 更新安装文档
- [ ] 更新 Skill 安装流程
- [ ] 关闭 Issue #1

---

## 参考

- [Issue #1: 用户反馈报告](https://github.com/legogogoagent/STM32-Complier-MCP/issues/1)
- [MCP官方规范](https://modelcontextprotocol.io/)
- [uvx文档](https://docs.astral.sh/uv/)
- [Playwright MCP (行业参考)](https://github.com/microsoft/playwright-mcp)
